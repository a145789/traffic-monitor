# Traffic Monitor

Windows 11 任务栏小组件，纯 Rust，无配置文件。嵌入任务栏系统托盘左侧，双行文字展示 CPU、内存、网速。

> [!NOTE]
> 本文件只保留跨模块、容易改错的 **INVARIANT**、必须遵守的 **CONVENTION**，以及非直觉的构建/发布门禁。具体 API 顺序、参数、文件归属和模块快照由源码就近说明；未来改法只放 [`docs/rfc/`](docs/rfc/README.md)，禁止把易漂移的实现清单复制回这里。

## 文档阅读策略（AI）

- 默认不读 `docs/`；其中的历史材料可能过时，不能当现行约束。
- `docs/rfc/` 是现行提案入口，可以且应该读；查待办用 `grep -rn "^Status: *proposed" docs/rfc/`。
- `docs/archive/` 仅在用户明确指定路径或要求查历史时读取。

## 必须保持的不变量

### 任务栏嵌入与 Explorer 重建

- 嵌入完成后的最终状态必须是任务栏的可见 child HWND：`WS_CHILD`、物理几何、Z 序、layered 属性和色键透明均有效。修改窗口样式后必须通过 `SetWindowPos`（含 frame-change 语义）应用；layered child 必须完成 layered 状态初始化。具体调用序列可替换，但不得留下半嵌入状态。
- `HWND_BROADCAST` 只投递给顶层窗口；任务栏 parent 被销毁时，跨进程 child 也会被级联销毁。因此依赖 `TaskbarCreated` 的架构必须保留一个不嵌入、隐藏且比主窗口更稳定的顶层恢复接收者。
- Explorer/任务栏重建后，所有绑定旧 HWND 的状态与能力都必须迁移或重建，监测、托盘、电源/会话通知和退出/更新交接不得退化为“假活”。
- 重建或嵌入的瞬态失败必须有独立于外部事件次数的恢复调度并最终收敛；同一失败序列只提示一次，周期重试必须静默，不能形成模态弹窗风暴。未完成有效嵌入前，不得把任务栏客户区坐标应用到顶层窗口。
- 应用主动销毁仍存活的 HWND 前，必须先注销绑定该 HWND 的 WTS 会话通知；窗口销毁后补做无效。

### 网速采样

- 每个采样周期只在符合产品资格判据的接口中选择一张，并展示该接口同一周期的上下行速率；不得累加多卡，也不得跨周期粘滞赢家。接口选择的具体身份来源与过滤启发式属于 collector 实现，不得把名称启发式冒充“物理网卡”证明。

### 更新、下载与安装交接

- 长期主进程不得因更新功能加载网络/加密依赖、执行下载校验、初始化更新专属 UI 或第三方 TSF/IME；这些工作必须留在可回收的短生命周期隔离边界内，避免 DLL、输入法状态与工作内存常驻。更新执行器也不得被主实例单例误判为重复实例。
- 手工检查在“已是最新”和检查失败时都给出结果；自动检查对无更新/失败保持静默；已有检查占用时保持静默，不启动第二个交互流程。
- 安装包信任必须绑定到最终启动的同一对象身份；校验后不得仅按路径另开文件重新建立信任，否则会重新打开 TOCTOU 窗口。哈希、签名、锁句柄或文件对象等算法可替换，但身份绑定不可删除。
- 安装器进入复制阶段前，旧实例必须已退出或以其它可靠机制终止，不能继续占用目标 exe。等待进程句柄、互斥量、安装器内优雅退出/终止等组合可替换。
- 安装器启动失败或 UAC 取消时，更新协调器必须尝试恢复主程序，并避免立即重复同一版本的确认交互；不得把“发起恢复”描述成“必然恢复成功”。
- 更新交接必须投递到 Explorer 重建期间仍稳定存在、不会随主窗口替换而丢失的接收者；否则消息丢失会让进行中状态永久占位。

### DPI、低功耗与恢复

- 有效 DPI 变化后，字体/位图等渲染资源、窗口物理宽高和任务栏内几何必须一致且合身；任一步失败都必须保持旧资源与窗口尺寸匹配，并可由恢复路径重试，不能留下“新位图 + 旧窗口”或永久错版。
- 休眠、锁屏、显示器关闭或本显示器全屏时停止高频监测/更新工作；离开这些状态后恢复活动采样。
- 多个暂停原因必须幂等收敛；只有真实的 paused→running 边沿才能重建 CPU/网络差分基线。通知、定时器或恢复调度的任何失败都不得造成永久冻结的“卡死假活”。全屏边沿独立恢复采样，但必须遵守同一基线语义。

### 资源、内存与并发

- 每个成功取得的 Win32 资源必须有一个明确 owner 和一条 exactly-once 配对释放路径；所有权移交、借用与销毁顺序必须可证明，失败路径也不得泄漏或 double-close。
- 传给同步 Win32 API 的 UTF-16 缓冲区必须 NUL 终止，并在 API 返回前保持有效且满足目标 API 的别名/可写性要求。
- 可恢复的生产错误不得因 panic 终止常驻进程（release 为 abort）；失败必须通过 `Result`、`Option`、早退或其他显式路径处理。
- 每个跨消息/跨线程事实只能有一个状态 owner；同步必须足以建立所需的 happens-before。新增共享事实时，在事实属主附近写明谁写、谁读、何时置位/清位以及并发到达如何收敛。

## 必须遵守的代码约定

- 新增说明性用户文案默认使用中文；既有英文品牌/标题、技术标识和协议字段不强制翻译。
- 生产路径避免 `unwrap` / `expect`，使用显式错误处理；测试与经审查的 mutex-poison 恢复路径例外。
- `unsafe` 注释必须说明真正支撑安全性的不变量（指针/句柄有效性、缓冲区生命周期、NUL 终止、别名、内存布局或失败歧义），不能只复述“句柄来自成功的 OS API”。

## 构建与发布门禁

### Rust / Cargo 改动

修改 Rust 源码、`build.rs` 或 Cargo 配置后，先运行 `cargo fmt`，再执行以下只读门禁；每条命令都必须成功，且人工审查输出中的 warning：

```bash
cargo fmt -- --check
cargo test --locked
cargo build --release --locked
cargo clippy --all-targets --locked -- -D warnings
```

Windows 集成冒烟（opt-in，需真实 Explorer/任务栏，不进必跑门禁）：`cargo test --locked -- --ignored --test-threads=1`（必须串行，全局窗口状态跨用例共享）。

- 依赖解析与 release 验证必须使用仓库已提交的同一解析结果：三条 Cargo 门禁均带 `--locked`，不得让本地命令顺手改写 `Cargo.lock`。
- 修改 `Cargo.toml` 的依赖或根包版本时，相应 `Cargo.lock` 变化必须同提交；`scripts/package.ts` 的临时 dev 版本改写与恢复是隔离流程，不得提交其中间状态。
- `Cargo.toml` 的 `rust-version` 是 MSRV 唯一来源，CI 的 MSRV job 必须与它对账；使用更高版本语言/API 前先通过该 MSRV 验证。
- 受支持的 release builder 必须在既定资源预算内完成构建。依赖面、profile、工具链或链接方式变化后必须重跑 release build；精确调优值及其适用范围只由 `Cargo.toml` / `build.rs` 就近维护。

### 发版与安装包

```bash
bun scripts/release.ts <version>   # 校验当前基线，更新三方版本与 lock，提交、打 tag 并推送
bun scripts/package.ts             # 本地常规安装包
bun scripts/package.ts dev         # 本地开发包；不参与升级，结束后恢复临时版本
```

- 发版脚本只能在干净的 `main` 上运行。若脚本已改写版本或创建本地 commit/tag 后失败，不要直接重跑；按脚本输出继续，避免二次改版本或重复提交。
- 发布 tag、`Cargo.toml` version、`installer.iss` AppVersion 必须表达同一版本身份；更新器与 release workflow 也必须定位同一资产及其摘要。改变协议时两端必须同批评审。
- 预期资产未全部上传、远端 `version.txt` 的版本或安装包摘要未与本次构建一致时，不得 publish；默认路径不得静默覆盖已存在的同名资产。
- 修复失败的发布必须显式选择目标 tag，并只对 draft 中本次将重传的既定资产执行 repair，例如：

```bash
gh workflow run release.yml --ref v<version> -f repair=true
```

  repair 不得触碰已 publish release 或无关资产。
